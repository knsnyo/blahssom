import { store } from 'src/app/(front-end)/__features/_config'
import withAuth from 'src/app/(front-end)/__features/user/ui/withAuth'

const Feature = {
  store,
  withAuth,
}

export default Feature
