import GlobalStyles from 'src/app/(front-end)/____shared/config/global-style'
import StyledComponentsRegistry from 'src/app/(front-end)/____shared/config/styled-components'
import { CUSTOM_FETCH, METHOD } from 'src/app/(front-end)/____shared/fetch'
import STYLE from 'src/app/(front-end)/____shared/style'

const Shared = {
  CUSTOM_FETCH,
  GlobalStyles,
  METHOD,
  StyledComponentsRegistry,
  STYLE,
}

export default Shared
